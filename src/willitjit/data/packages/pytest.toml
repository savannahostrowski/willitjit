[package]
rank = 22
name = "pytest"
downloads = 1091772181
repository = "https://github.com/pytest-dev/pytest.git"
ref = "9.1.1"
release_version = "9.1.1"
release_date = "2026-06-19T10:58:31.347074Z"
install = [
  ["-m", "pip", "install", "build"],
  ["-m", "build", "--sdist", "--outdir", "dist"],
  ["-m", "pip", "install", "dist/pytest-{release_version}.tar.gz[dev]"],
]
test = ["-P", "-m", "pytest", "testing"]
timeout_seconds = 2400
note = "Upstream default tox unit lane: install the release sdist with its dev extra, then run testing directly in the verified survey environment. No nested tox, coverage or local test deselections."
guidance = [
  "https://github.com/pytest-dev/pytest/blob/cf470ec0bf7eb89cd97dd56df4859eae5db46447/tox.ini",
]
environment = { "PYTHONWARNDEFAULTENCODING" = "1" }
