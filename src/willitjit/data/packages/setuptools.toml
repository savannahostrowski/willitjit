[package]
rank = 9
name = "setuptools"
downloads = 1563563625
repository = "https://github.com/pypa/setuptools.git"
ref = "v84.0.0"
release_version = "84.0.0"
release_date = "2026-08-08T18:27:56.719708Z"
install = [["-m", "pip", "install", "-e", ".[test,core,cover,enabler]"]]
test = ["-m", "pytest", "-p", "no:perf", "-p", "no:cov"]
environment = { "PYTHONWARNDEFAULTENCODING" = "1", "SETUPTOOLS_ENFORCE_DEPRECATION" = "0" }
timeout_seconds = 2400
guidance = [
  "https://github.com/pypa/setuptools/blob/72e919a8b10aaafc041205d4e3ae0e6a2e1e5f87/pyproject.toml",
  "https://github.com/pypa/setuptools/blob/72e919a8b10aaafc041205d4e3ae0e6a2e1e5f87/tox.ini",
  "https://github.com/pypa/setuptools/blob/72e919a8b10aaafc041205d4e3ae0e6a2e1e5f87/pytest.ini",
]
note = "Uses upstream tox's editable test/core/cover extras and environment defaults, preserving pytest doctests, warning rules and pytest-enabler's xdist configuration. The cover extra supplies coverage.exceptions.CoverageWarning referenced by pytest.ini; coverage measurement remains disabled. Pytest-perf benchmarks, style, typing and the separate integration lane are outside scope. Requires stdlib test.support."
