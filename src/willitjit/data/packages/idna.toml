[package]
rank = 6
name = "idna"
downloads = 1760742555
repository = "https://github.com/kjd/idna.git"
ref = "v3.19"
release_version = "3.19"
release_date = "2026-08-18T05:14:22.343598Z"
install = [
  ["-m", "pip", "install", "-e", "."],
  ["-m", "pip", "install", "pytest>=8.3.2", "hypothesis>=6.141.1"],
]
test = ["-m", "pytest", "tests"]
timeout_seconds = 600
environment = { PYTHONUTF8 = "1" }
note = "Forces UTF-8 subprocess I/O so the CLI can emit decoded IDNs on Windows, matching Python's UTF-8 CI mode."
