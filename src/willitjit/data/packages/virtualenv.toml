[package]
rank = 76
name = "virtualenv"
downloads = 532873318
repository = "https://github.com/pypa/virtualenv.git"
ref = "21.7.4"
release_version = "21.7.4"
release_date = "2026-08-10T22:54:31.515567Z"
install = [
  ["-m", "pip", "install", "tox-uv"],
]
test = ["-m", "tox", "run", "-e", "3.14", "--", "tests", "-m", "not slow", "-p", "no:xdist"]
timeout_seconds = 2400
isolate_home = true
windows_native_line_endings = true
note = "Uses upstream's Python 3.14 tox-uv environment, which builds and tests the pinned checkout as a wheel. Windows uses native checkout line endings because the activation suite verifies them."
